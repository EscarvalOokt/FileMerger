<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource Brush.DataGridBackground}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="RowBackground" Value="{StaticResource Brush.DataGridBackground}" />
        <Setter Property="AlternatingRowBackground" Value="{StaticResource Brush.DataGridAltRow}" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="HeadersVisibility" Value="Column" />
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource Brush.DataGridHeader}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="8,6" />
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.DataGridRowHover}" />
            </Trigger>

            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="Selector.IsSelectionActive" Value="True" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.DataGridRowSelected}" />
                <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
            </MultiTrigger>

            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="Selector.IsSelectionActive" Value="False" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.DataGridRowSelectedInactive}" />
                <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridRowHeader">
        <Setter Property="Width" Value="24" />
        <Setter Property="Background" Value="{StaticResource Brush.DataGridRowHeaderBackground}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.TextSecondary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridRowHeader">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Border x:Name="SelectionIndicator"
                                    Width="3"
                                    HorizontalAlignment="Left"
                                    Background="{StaticResource Brush.InputBorderFocus}"
                                    Visibility="Collapsed" />
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder"
                                    Property="Background"
                                    Value="{StaticResource Brush.DataGridRowHeaderHover}" />
                        </Trigger>

                        <Trigger Property="IsRowSelected" Value="True">
                            <Setter TargetName="RootBorder"
                                    Property="Background"
                                    Value="{StaticResource Brush.DataGridRowHeaderSelected}" />
                            <Setter TargetName="SelectionIndicator"
                                    Property="Visibility"
                                    Value="Visible" />
                            <Setter Property="Foreground"
                                    Value="{StaticResource Brush.TextPrimary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Padding" Value="6,4" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
            </Trigger>

            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource Brush.DataGridCurrentCellBorder}" />
                <Setter Property="BorderThickness" Value="1" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DataGridCellTextStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="ToolTipService.ShowDuration" Value="60000" />
        <Setter Property="ToolTip"
                Value="{Binding RelativeSource={RelativeSource Self}, Path=Text}" />
    </Style>

    <Style x:Key="RelativePathCellStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource DataGridCellTextStyle}">
        <Setter Property="ToolTip">
            <Setter.Value>
                <ToolTip Placement="Mouse" MaxWidth="1000">
                    <StackPanel>
                        <TextBlock FontWeight="SemiBold"
                                   Text="Full path:"
                                   Margin="0,0,0,4" />
                        <TextBlock Text="{Binding FullPath}"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </ToolTip>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SourcePathCellStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource DataGridCellTextStyle}">
        <Setter Property="ToolTip">
            <Setter.Value>
                <ToolTip Placement="Mouse" MaxWidth="1000">
                    <TextBlock Text="{Binding Path}"
                               TextWrapping="Wrap" />
                </ToolTip>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ValidationMessageCellStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource DataGridCellTextStyle}">
        <Setter Property="ToolTip">
            <Setter.Value>
                <ToolTip Placement="Mouse" MaxWidth="900">
                    <TextBlock Text="{Binding Message}"
                               TextWrapping="Wrap" />
                </ToolTip>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ValidationCodeCellStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource DataGridCellTextStyle}">
        <Setter Property="ToolTip"
                Value="{Binding Code}" />
    </Style>

    <Style x:Key="SkipReasonCellStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource DataGridCellTextStyle}">
        <Setter Property="ToolTip">
            <Setter.Value>
                <ToolTip Placement="Mouse" MaxWidth="900">
                    <TextBlock Text="{Binding SkipReason}"
                               TextWrapping="Wrap" />
                </ToolTip>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>