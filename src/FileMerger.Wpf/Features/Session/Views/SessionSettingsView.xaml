<UserControl x:Class="FileMerger.Wpf.Features.Session.Views.SessionSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <GroupBox Header="Session">
        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Margin="0,0,8,8"
                       VerticalAlignment="Center"
                       Text="Session name:" />
            <TextBox Grid.Row="0"
                     Grid.Column="1"
                     Margin="0,0,0,8"
                     Style="{StaticResource CompactTextBoxStyle}"
                     Text="{Binding SessionName, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Text="Output file:" />
            <Grid Grid.Row="1"
                  Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="90" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         Margin="0,0,8,0"
                         Style="{StaticResource CompactTextBoxStyle}"
                         Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="1"
                        Command="{Binding BrowseOutputCommand, ElementName=Root}"
                        Content="Browse" />
            </Grid>
        </Grid>
    </GroupBox>
</UserControl>